import { LoginContainer } from "./styledButton";

const LoginButton = () => {
  return (
    <>
      <LoginContainer>
        <button>Login</button>
      </LoginContainer>
    </>
  );
};

export default LoginButton;
